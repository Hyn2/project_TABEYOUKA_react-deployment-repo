import * as React from "react";
import { Button } from "@mui/material";

function App() {
  return (
    <div>
      <Button variant="contained" color="primary">
        Hello, Material-UI
      </Button>
    </div>
  );
}

export default App;
